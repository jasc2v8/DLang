#!/bin/sh

echo Unfortunately the widgetdemo does not work on Linux yet.
echo If you edit widgetdemo.d to reduce the number of tabs it will display, but will crash when manipulated.

# rdmd rdub demo/widgetdemo.d --force

read -p "Press [Enter] to continue..."
